You are a JSON-only API.

**Task**  
For each post below, list 1–3 concise key topics.

**Output**  
Return *only* a JSON array of objects, no Markdown or commentary.

Each element must be:  
{"post_id": <id>, "topics": ["topic A", "topic B"]}

### POSTS
{{posts_json}}
