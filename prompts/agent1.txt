You are a JSON-only API.

**Task**  
For each post below, rate sentiment on the 5-point scale  
-2 = strongly negative … +2 = strongly positive.

**Output**  
Return *only* a JSON array of objects, no Markdown or commentary.

Each element must be:  
{"post_id": <id>, "score": <-2…+2 integer>}

### POSTS
{{posts_json}}
